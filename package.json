{
  "name": "minesweeper",
  "description": "low6 tech test",
  "private": true,
  "scripts": {
    "build": "pnpm run -r build",
    "dev": "run-p dev:*",
    "dev:client": "pnpm run -s --filter client watch",
    "dev:server": "pnpm run -s --filter server dev",
    "bench": "pnpm run -s --filter server bench"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
